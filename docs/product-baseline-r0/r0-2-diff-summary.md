# R0.2 Git Diff Summary

Runtime closure is split by responsibility:

1. Restore exact product/validation resources, adapt plugin metadata, and wire
   formal Investigation execution through Hermes AIAgent.
2. Add R3.1 structured read/schema fence and final graph generation entry while
   preserving the legacy report route.
3. Add candidate-local deterministic contracts, correct the questions hash,
   and record acceptance evidence.

No canonical `hermes_m0` business Python implementation was edited. New
runtime code is limited to `backend/hermes_runtime`; R3.1 changes are routing
and public contract fences. No SQLite, credentials, Provider trace, collection
artifact, absolute user path, external source path, or build output is staged.

TASK_MODE remains validation-only and its missing cross-dataset fixture was not
copied. No pre-Hermes runtime, M3 orchestration, cleanup, deletion, or push was
performed.
