# M3 历史消息渲染修复

用户截图对应会话 `investigation-session:a9c2326b6b4a4b2cb2d5c53bc3028379`。原始保存回执是合法四列表格；旧前端逐行过滤内部字段，删除了包含“修订 ID”的表头以及审核规则记录，留下分隔线和词库记录，导致 Markdown 表格失效。带结构化规则展示回执的消息保留原始正文，却没有在卡片说明区应用展示过滤；无结构化快照时还会直接输出纯文本。

本次修改：

- 保存表格按列隐藏操作/修订编号，保留两份资源和各自版本；字段—值表格仅移除明确的元数据行。支持无外侧竖线、转义竖线和列对齐。
- 卡片说明使用与普通创建消息相同的展示处理，词条内部编号与已知实现字段转换为用户可读内容。规则快照继续由原结构化数据展示，不修改规则或豁免。
- 无结构化快照的旧规则消息通过 Markdown 展示，保留其原始规则文本，不凭文字伪造卡片或新版本。
- 引用和普通代码块保留字面内容；不对已发布报告问答应用创建消息的元数据过滤。

验证：13 项逻辑/语义 HTML 测试通过；TypeScript 与 Vite 生产构建通过；已有浏览器用例的旧纯文本预期改为 Markdown 预期，但本次未启动该独立浏览器测试套件。使用内置浏览器实查历史会话：保存回执恢复为“资源／新版本”两列，两行均为 v2；冲突说明中的三个处理选项完整；规则卡片可展开全部 6 条规则及 3 条通用豁免；刷新后恢复。

只更新 3178/8178 实验服务，在没有执行中 Run 或模型轮次时重启。A/B、身份和 worker ready 检查通过；重启前后全部实验 Run 与对话轮次的 ID/状态保持一致，没有新增采集或模型调用。K2/3168 未操作。

最终构建：`6b6b082cd01811dfd753d74df0cf424bb4dbaa87b9d29df9a1442da944289695`。

截图：[保存回执](evidence/m3-rendering-20260911/save-receipt.png)、[冲突说明](evidence/m3-rendering-20260911/conflict-and-card.png)。

范围限制：这是前端结构保全与已知内部字段的展示修复，不保证模型任意输出的错误 Markdown 都能自动纠正。卡片仍由真实结构化回执驱动，普通解释、冲突说明和保存回执使用 Markdown，因此二者会按内容类型并存。
