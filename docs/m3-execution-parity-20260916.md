# M3 底层行为收敛与主界面验收（2026-09-16）

## 结论与范围

代码修复、完整门禁与主界面账号池恢复实测通过；平台验证仍阻断真实内容采集，尚不能判定端到端验收通过或晋升正式基线。

开发分支 `codex/douyin-ops-controls-20260916`，HEAD `a925969d78e570bbd99413dfa2e2b0d5283811da`；保留工作树已有修改，未提交。未改正式2134c7b基线、3198/8198、正式数据库或密钥。隔离环境只读复用既有模型配置。

不拆服务、不重建M3。保留用户确认、幂等、冻结Run/Job/revision校验、真实供应商和报告完成校验。本记录取代旧文档中“逐任务参数表单”的设计；旧 `/tasks` 记录只算底层预验收。

## 已实施

- 业务入口下“采集与分析设置”保存一套应用参数。确认卡只读显示实际生效值；对话工具不再提供逐任务超参字段。
- 设置单行持久化，内部修订号防止并发覆盖；启动确认校验设置版本，过期预览须重确认。这不是用户管理多个参数版本。
- 确认启动时冻结参数，修改设置不影响已有任务/恢复。普通任务接口同样读取已保存统一设置及安全上限，自动选择可用账号；历史未配置环境保持兼容。
- 删除M3对验证/空结果轮换的排除条件，两种入口走同一公共策略。区分auth、verify、rate-limit；每次执行遍历当轮全部可用账号且每个至多一次，验证冷却不误标过期，不循环换号。
- 可恢复失败以结构化字段记录失败阶段、错误码、可恢复性和恢复动作；M3只负责同一Run/Job的`FAILED -> 恢复中 -> 完成或最新FAILED`衔接，账号轮换仍由公共采集流程负责。
- 账号页将active但处于冷却期的账号显示为“冷却中”，从当前可用数排除；当同平台没有可用账号时，主界面“继续采集”禁用。
- `control.execution_account` 记录实际账号，M3原始配置不改写；恢复校验实际接替账号，冻结一致性仍校验。切换时读取最新落库检查点，沿用跳过列表。
- 采集阶段先确认结束状态再等待分析线程；保留epoch防旧线程覆盖、分析锁和原子领取。分析暂停/停止不清空队列。
- 更正账号失败提示：平台验证不再显示成平台设置失败，错误日志不再被脱敏逻辑改成“校验完成”。

## 自动回归

候选提交前最终 `/bin/sh scripts/douyin-ops-gate.sh`：**354 passed，23 subtests passed**，TypeScript与Vite production build成功。只有既有弃用和大包警告。脚本使用固定Node/Python路径，不依赖npm，不运行正式控制器。

覆盖：两入口verify/empty/auth/rate-limit对照、停止后不轮换、四账号池逐个至多一次、结构化失败恢复门禁、同一Run/Job二次失败、检查点恢复、原账号后续过期时接替账号仍可恢复、冻结篡改拒绝、统一设置覆盖入口参数、旧预览拒绝、幂等重放保持原参数、错误日志不转成功。

候选提交前全量回归一轮：**1274 passed、30 skipped、8 failed、11 errors**。复核结果：

- 6项爬虫调度/共享执行失败由默认 `external/MediaCrawler` 路径缺失造成；显式指定隔离 `MEDIACRAWLER_DIR` 后，涉及本轮公共调度/执行的相关重跑 **7 passed**。
- 余下2项在test_pass_investigation.py：Mock缺少真实db_path；评论覆盖率断言未包含当前新增字段。相关文件本轮未修改；未通过项保留，不以排除宣称全绿。
- 11个historical report setup error缺失历史冻结归档SQLite。真实 `/api/historical-report-workspaces` 也有500；未伪造归档或修改正式资源绕过。

因此尚不能宣称所有其他功能已验证完整可用。

## 主界面真实证据

- 页面：`http://127.0.0.1:3199/investigation/investigation-session%3A735b803a00084a87a509de1ad82d091c`
- Run：`investigation-run:87efa74ae4684f9ba35c024e25046fe0`
- Job：`m3-843f98377f5d847571b2`
- 真实Hermes对话 → 临时规则展示与确认 → 建草稿 → 生成配置 → 点击确认启动；没有用旧页面或API代替主界面创建。
- 临时规则仅供隔离验收，不发布正式库；不因民族身份/正常婚恋表达判风险，不推断身份。
- 冻结关键词仅“维汉夫妻”，采集2、分析2、评论1/帖、速度1/分钟、并发1、批次1。未真实采集盘口词。
- 15:14:48真实MediaCrawler开始搜索，主界面显示独立控制按钮。
- 15:15:02平台验证，错误码crawler_account_verification_required。主账号active+verify，冷却至15:20:02，未标expired；当时备用账号expired，故未继续轮换。
- 暂停分析点击与失败发生时间相近，不能计为独立控制通过。
- 入库0、审核结果0、无报告；完整审核、不重复审核、重启恢复均未获得本次真实证据。
- 更正提示后读取同一Run，错误日志明确显示平台验证/冷却，不再显示校验完成。

### FAILED恢复与四账号池实测（15:54）

- 从正式冻结库只读迁移`dk账号`和`wht抖音`到隔离库；使用正式密钥解密后以隔离密钥重新加密，没有复制密钥或修改正式库。隔离库迁移前备份为`audit_index.before-dk-wht-migration-20260916-155253.sqlite3`。
- 两个账号沿用原账号ID并各自创建独立CloakBrowser指纹：`wht抖音` seed 10002，`dk账号` seed 10003。账号页显示4个账号均可用后，从主界面点击原Job的“继续采集”。
- 保留原Run `investigation-run:87efa74ae4684f9ba35c024e25046fe0`、原Job `m3-843f98377f5d847571b2`、关键词“维汉夫妻”和page 1检查点；页面先显示“正在从检查点恢复”。
- 公共流程实际顺序为`zc常用账号 -> dk账号 -> wht抖音 -> zc不常用账号`。四个账号各尝试一次，日志剩余数依次为3、2、1、账号池耗尽；两个迁移账号的独立指纹浏览器均真实启动。
- 四个账号均再次触发平台验证：保持active、不标expired，分别冷却至15:59。本轮回到结构化可恢复FAILED：`stage=crawl`、`recoverable=true`、`recovery_action=continue_crawl`。
- 账号页显示当前可用0、需要处理4和每个账号的“冷却中”截止时间；调查页“继续采集”禁用。入库0、审核0、报告0，不宣称内容链路通过。

## 交接与待验收

本节记录的是当轮实测现场。候选铆钉前复核时，8199 API仍在，3199前端已停止，当前没有采集器或CloakBrowser残留。正式3198/8198保持原进程和正式数据不变。

冷却结束或用户重新登录后，可再次从主界面恢复同一Job，不新建或扩量。需补验真实内容入库、暂停分析时采集继续、停止采集即时确认且分析继续、同会话/Job重启恢复、完整审核/报告、completed内容不重复审核。再遇验证应保持FAILED并交还用户，不绕过。
