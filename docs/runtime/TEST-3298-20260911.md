# 独立测试环境 · 3298

入口：<http://127.0.0.1:3298/investigation>，API：8298。

代码基于冻结提交 `f3ea3b08aa36e4d7c284ad0e50b2164f8fe61dd4`（`formal-baseline-20260911` 标签所指提交），分支为 `codex/test-runtime-3298-20260911`。未迁入 3198 后续的会话删除改动。

- 独立代码：`/Users/ext.wanghongtao6/Documents/Codex/projects/xhs-audit-agent-test-3298-20260911`。
- 独立数据：`/Users/ext.wanghongtao6/Documents/Codex/runtime/xhs-audit-agent-test-3298-20260911`。
- 数据来源：`/Users/ext.wanghongtao6/Documents/Codex/runtime/backups/xhs-audit-agent-formal-20260911-frozen`。
- 系统服务：`com.xhs-audit.formal-test-3298-20260911`，管理前端、API、worker，登录后自动启动。
- 原 A/B/C 和已验收单帖会话随冻结备份保留；新数据写入测试副本。Python 和采集依赖环境复用本机已安装依赖，前端依赖独立安装。
- 沿用冻结版本的默认单帖、3 条评论配置。页面顶部仍是冻结版本的“正式工作台”文案，请通过地址中的 **3298** 区分测试环境。

## 操作

使用专用入口，避免误用 `formal-runtime.sh` 默认的 3198 数据目录：

```bash
bash /Users/ext.wanghongtao6/Documents/Codex/runtime/xhs-audit-agent-test-3298-20260911/control.sh status
bash /Users/ext.wanghongtao6/Documents/Codex/runtime/xhs-audit-agent-test-3298-20260911/control.sh check
bash /Users/ext.wanghongtao6/Documents/Codex/runtime/xhs-audit-agent-test-3298-20260911/control.sh restart
```

修改测试代码后先执行同一入口的 `build`，再 `restart`。停止用 `stop`；停止后恢复用 `install-service`。这只管理 3298/8298。

## 旧端口整理

按用户要求保留 **3168/8168**、**3198/8198** 及其 worker。已验证用户指定的 3168 会话和 3198 历史报告 A 的业务接口返回 200。

检查任务、调查运行和对话状态后，停止空闲旧服务：

| 前端 | 后端 |
| --- | --- |
| 3001 | 8000 |
| 3002 | 8012 |
| 3003 | 已无监听 |
| 3004 | 8014 |
| 3128 | 8127 |
| 3148 | 8148、8149、8147 |
| 3151 | 8151 |
| 3178 | 8178 |
| 3188 | 8188 |
| 无 | 8137 |

相应旧 worker 一同停止，未删除数据库、代码、未提交修改或备份。主库的 `stopping` 记录最后更新时间为 2026-07-18，属于旧状态；本次不修改任务记录。

完整进程、工作目录、服务名和停止前状态记录在测试运行目录的 `receipts/stopped-idle-services.json`。旧系统服务已 `disable` 并 `bootout`，避免登录后再次自动占用端口；plist 文件保留。其他项目及系统服务不在本次停止范围。

需要恢复某个已停系统服务时，从收据选择对应服务名，先 `launchctl enable gui/$(id -u)/服务名`，再 `launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/服务名.plist`。手工启动的旧服务应使用原环境启动说明及配置恢复，不要仅根据端口启动一个无配置后端。

## 验证

- 5 个 SQLite 数据库在线备份并通过完整性检查，4 份报告归档摘要一致。
- 固定锁文件安装依赖、TypeScript 检查、Vite 构建通过。
- 前端与后端身份、独立数据目录、构建指纹、A/B/C 报告读取和 worker 心跳检查通过。
- 浏览器正常显示测试工作台、历史会话和报告 A 内容。
- 停止旧服务后再次确认 3168、3198、3298 及对应后端保持监听，旧端口均已释放。
- 本次验证环境可用性，没有重新发起付费模型或真实采集任务；大批量压力验收仍未执行。
