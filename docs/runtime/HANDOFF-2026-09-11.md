# A/B/C 报告展示交接文档

交接日期：2026-09-11。本文记录当前实际状态；如与旧的整合计划、进度描述冲突，以本文及用户最新要求为准。

## 1. 交接结论

- 当前可使用的 A/B/C 展示入口为 http://127.0.0.1:3188/investigation 。
- 已处理报告展示、全部分析记录、正常研判详情跳转，以及 C 部分视频黑屏。
- 不能称为“所有环境迁移完成”，也不能称为“所有报告已通过同一条冻结流程且能力完全一致”。
- 用户已明确放弃旧会话迁移：从本次范围移除，不再作为待办或验收缺口。原数据保留；本次创建的初始快照与四轮迁移副本已于用户确认后删除。
- C 尚无 A/B 同样的前端用户活动功能。上一轮对这项功能仅做了只读检查，没有实现或新增数据。本次交接不提供个人活动追踪的实现方案。
- 本文初次交接只编写文档。后续用户明确授权删除本次备份，已删除指定五个目录；没有调用 Qwen、生成新报告或调整运行服务。

## 2. 代码与运行环境

| 项目 | 当前值 |
| --- | --- |
| 工作目录 | `/Users/ext.wanghongtao6/.codex/worktrees/2018/xhs-audit-agent-demo` |
| 分支 | `codex/formal-entry-20260911` |
| HEAD | `14256523f7506714d1c3e3310535c4eb3567ba24` |
| 前端 | `http://127.0.0.1:3188` |
| API | `http://127.0.0.1:8188` |
| 环境标识 | `xhs-audit-formal-candidate` |
| 固定运行目录 | `/Users/ext.wanghongtao6/Documents/Codex/runtime/xhs-audit-agent` |
| 业务数据目录 | 上述运行目录下的 `data/` |
| 当前构建指纹 | `c664069f5009f1fdf1e3d7966269b0a0e5a14c1d2c0b2267a2e774dfbc8c0f38` |

交接时实际读取了前端 `/__runtime` 和后端 `/api/m3-runtime`，两端环境标识、目录、端口及构建指纹一致。

**HEAD 不等于当前运行功能的完整版本。** 工作区仍有已修改文件和未跟踪文件，包括历史进度卡、历史分析记录读取、媒体处理模块等。仅检出上述提交会漏掉当前修复；不得先清理工作区再开始交接。尚未完成最终提交、版本冻结及唯一正式入口切换。

`candidate-processes.json` 记录当前候选进程，交接时为 API 47096、前端 47097；PID 会变化，不应作为永久配置。当前没有完成系统托管，也不承诺机器重启后自动恢复。

旧的 3148、3151、3168、3178 属于此前不同环境，本文未重新验证其在线状态。本次文档编写未修改或重启这些环境。

## 3. 报告与数据位置

| 报告 | 工作区地址 | 已保存分析记录 |
| --- | --- | ---: |
| A | `http://127.0.0.1:3188/investigation/historical-report-a` | 201 |
| B | `http://127.0.0.1:3188/investigation/historical-report-b` | 304 |
| C | `http://127.0.0.1:3188/investigation/historical-report-c` | 219 |

固定运行目录内：

- `archives/report-a.sqlite3`、`report-b.sqlite3`、`report-c.sqlite3`：历史报告归档。
- `archives/report-c-manifest.json`：C 的来源登记资料。
- `data/`：当前入口使用的数据库、会话等运行数据。
- `data/outputs/`：当前入口使用的媒体与证据素材。
- `config.json`：当前工作目录、运行时和端口配置。
- `build.json`、`public.json`：构建和入口身份资料。
- `logs/candidate-api.log`、`logs/candidate-frontend.log`：候选服务日志。
- `media-archive-receipt-20260911.json`：本次补齐 C 素材副本的清单。

不要把密钥配置内容写进交接消息、提交或公开文档。

## 4. 当前展示行为及用户确认的要求

### 完成卡片

A/B/C 复用既有视觉样式和四阶段流水线组件。卡片包括：

- “调查流水线已完成”和“研判完成”。
- 进入研判、待分析、分析中、已完成四项统计。
- 四个已完成节点，包括绿色报告节点。
- 三条分析记录预览、“查看全部分析记录”和“查看研判依据”。

用户不接受这次额外增加的“展开依据”入口，已从 A/B/C 的历史进度卡移除。查看研判依据直接进入完整审核详情，不先弹这个额外抽屉。原项目其他位置已有的抽屉组件不等于本次新增交互，未做全项目删除。

用户明确要求：完成卡片内不展示未完成帖子、缺译文及额外说明。已按要求移除这些卡片内容。C 的完成统计是**报告内 219 帖的范围**，不是宣称原候选 224 帖全部审核成功，也不是重新执行产生的时间线。报告正文、原始数据和已有对话没有因此被删除或改写。

前端在本次调整前确实新增过不同的卡片布局；即使没有修改全局 CSS，也不能称为“完全没有改样式”。现已恢复使用原卡片结构与样式类。

### 分析记录与详情

- 全部分析记录来自已保存的报告快照。
- 详情链接同时绑定报告版本与帖子引用，不能只拼接一个看似相同的数字结果 ID。
- C 有多个来源任务，逐帖跳到对应任务的结果，不统一跳到用户曾提供的示例结果 1083。
- 完整详情展示已有正文、媒体、评论与审核依据，不重新审核。

### C 视频修复

实际复现了 C 第 219 条“维汉夫妻倡导民族团结”的黑屏：视频保存的是旧采集目录绝对路径，接口返回 `400 Invalid asset path`。

已完成：

1. 查找快照引用的原素材，补齐到当前固定运行目录中的归档副本。
2. 149 个外部素材引用均找到并复制，其中 65 个 MP4、84 个 JPEG。
3. 接口仅把当前报告帖子确实保存的路径映射到受控归档副本，不直接开放任意旧绝对路径。
4. 增加视频分段读取支持，便于播放和定位。

原快照和原素材未覆盖。补齐副本时核对了文件大小；不是对全部媒体逐帧检查，也没有逐一人工播放全部 65 个视频。

## 5. 验证证据与限制

### 已实际验证

- A：201 条记录列表、完整详情及视频。
- B：304 条记录列表；高风险筛选后，记录 149 的完整详情、评论和视频。
- C：记录列表；一个含 578 条评论、6 条风险评论依据的详情及约 31 秒视频。
- C 新卡片：四个流水线节点均为完成状态；无额外“展开依据”按钮；直接详情跳转可用。
- C 第 219 条修复后：浏览器加载首帧，视频尺寸 1080×1920，时长 67.615 秒；实际点击播放后时间从 0.17 秒前进到约 10.96 秒，随后暂停。
- 视频接口：实际收到 `206 bytes 0-1023/18032703`、`video/mp4`。
- 最近一次媒体回归测试：2 项通过，覆盖普通归档文件、旧绝对路径映射、分段读取、尾部读取、越界范围、未授权帖子与未登记路径拒绝。
- 最近一次前端 TypeScript 检查和 Vite 生产构建通过；仍有构建产物体积提示。
- 最近一次 `git diff --check` 通过。

### 不能扩大解释为已验证的事项

- 没有逐帖人工验收 A/B/C 全部媒体和全部详情。
- A/B 的列表和详情验证发生在最后一次完成卡片样式恢复之前；最后一次样式恢复后，单独确认的是 C 卡片和跳转。
- 当前没有完整的新单帖端到端验收结论，不能推断未来流水线报告自动具备 A/B 所有功能。
- 曾有个别问答样例验证通过，但不能等同于所有账号、所有问题或前端用户活动已完整验收。
- C 前端用户活动仍未实现；既有自然语言问答能力与前端模块不是同一项交付。

## 6. C 用户活动请求的交接状态

只读核对发现：A/B 的报告接口返回可用的账号活动数据；C 返回不可用。C 当时采用汇总已有审核结果的专用生成路径，没有完整沿用 A/B 的账号活动环节。因此问题不只是隐藏了一个前端按钮，也不能只更换模板解决。

此前把“报告生成成功”“问答样例通过”说成报告能力已完整接入，表述过满，应纠正。尚不能保证所有报告遵守同一冻结流程并具备一致能力。


## 7. 已取消范围及后续状态

**已取消，不作为后续待办：**

- 旧会话迁移、旧任务恢复和迁移副本安装。
- 原计划中将所有历史实验数据合并至当前运行库的工作。

固定运行目录下 `migration/20260911-initial` 和 `migration/merge-01` 至 `merge-04` 已按用户确认删除。这些副本从未正式安装。删除后核对了27个原库、当前运行库及报告归档文件仍在。删除回执为 `migration/backup-deletion-receipt-20260911.json`；小型验证记录和取消说明保留。不得因接手旧计划而自动恢复历史执行队列。

**尚未交付的环境收尾：**

- 把当前经过审阅的修复形成可复现提交并冻结版本。
- 明确唯一正式入口和系统托管、重启恢复方式。

这些事项与“旧会话迁移”分开。本文只交接状态，没有执行正式切换或重新启动这些工作。

## 8. 主要修改文件与资料

相对当前工作目录：

| 文件 | 用途 |
| --- | --- |
| `Audit_assistant/src/features/investigation/HistoricalProgressCard.tsx` | A/B/C 完成卡片、记录预览、直接详情跳转 |
| `Audit_assistant/src/features/investigation/historicalAnalysisRecords.ts` | 历史分析记录加载 |
| `Audit_assistant/src/features/investigation/AnalysisRecordsPage.tsx` | 全部记录页面及历史详情跳转 |
| `Audit_assistant/src/features/investigation/InvestigationCenterArea.tsx` | 历史进度卡接入 |
| `Audit_assistant/src/features/investigation/m3AnalysisRecords.ts` | 报告版本与帖子绑定的详情地址 |
| `Audit_assistant/src/features/task-outputs/TaskOutputDetailPage.tsx` | 使用冻结报告资料展示完整详情 |
| `Audit_assistant/src/services/reports.ts` | 报告详情及素材地址处理 |
| `backend/api/historical_reports.py`、`backend/historical_reports/service.py` | 历史分析记录接口 |
| `backend/reporting/store.py` | 从冻结快照读取完整审核详情 |
| `backend/api/reporting.py`、`backend/reporting/media.py` | 报告素材授权、归档路径和分段读取 |
| `scripts/m3_environment.py`、`scripts/m3_frontend.mjs` | 当前候选环境身份、构建和启动机制 |
| `tests/test_historical_report_demo.py` | 历史报告与媒体回归测试 |
| `docs/runtime/formal-entry-integration-progress.md` | 历史进度记录，包含已被取消的旧计划 |

以上是展示与媒体相关重点，不是完整工作区差异清单。仍有其他先前修改和未跟踪文件，应以 `git status --short`、`git diff` 和实际文件为准，不要直接丢弃。

建议交接时先做只读核对：确认工作区差异、3188/8188 的身份一致性，以及用户当前保留的范围；不要将旧文档中未勾选的迁移步骤当作新的授权。
