# 正式入口整合验收记录（进行中）

2026-09-11。此文记录候选进展，不表示正式切换完成。

后续更新：用户已确认删除本次创建的 `migration/20260911-initial`、`merge-01`、`merge-02`、`merge-03`、`merge-04`，现已删除。原库、当前运行库与报告归档保留；下文关于保留这些迁移副本的描述仅为历史记录。

最新交接以 [HANDOFF-2026-09-11.md](/Users/ext.wanghongtao6/.codex/worktrees/2018/xhs-audit-agent-demo/docs/runtime/HANDOFF-2026-09-11.md) 为准；本文保留历史过程，其中旧会话迁移已被用户取消，C 前端用户活动尚未实现。

## 2026-09-11 用户缩小范围（当前有效）

用户取消旧会话迁移及其他整合工作，本轮仅处理 A/B/C 的展示、分析记录展开与正常研判依据页面。下文的原整合范围仅作历史记录，不再作为待办执行。

- `migration/merge-04` 仅为迁移副本，未安装到运行库；后续迁移、正式切换、系统托管和新单帖验收已停止。
- 已有原数据、备份及候选环境保留，不删除。
- 3188 继续提供 A/B/C 展示；C 的账号查询已修复，真实问答验证 A/B 各0条、C 1条。
- A/B/C 已保存媒体已复制到固定运行目录。C 正常详情的31秒视频、578条评论及6条证据已在浏览器验证。
- A 的201条列表与完整详情、B的304条列表及高风险记录详情、视频均已在浏览器验证。
- 按用户截图恢复 A/B/C 完成卡片的原有布局，去掉新增的“展开依据”抽屉入口及卡片内失败/译文说明。“查看研判依据”直接进入完整详情。C 按报告内219帖展示完成统计。
- C 的第219条视频黑屏已复现：快照引用旧采集目录的绝对路径，被资源接口拒绝。已把149个外部素材（65个MP4、84个JPEG）复制至按来源任务隔离的 report-assets 归档；快照与原文件不变。接口仅允许当前帖子已保存的路径映射到归档副本，并支持视频 Range 请求。两个媒体回归测试通过，第219条视频首帧、67.615秒时长及实际播放已验证。

## 原已授权范围（已被上述要求替代）

- 整合单帖两项修复、规则词典功能、Report C 和完整启动机制。
- 数据及服务固定到代码目录之外，保留原记录和 A/B/C、持续问答。
- 保留 3148 侧栏全部旧会话（包括失败、中断、201帖完成及已生成报告），不是只保留14帖会话。
- 新单帖会话创建、确认、采集审核、报告、后续问答完整验收。
- A/B/C 四阶段流水线、全部分析记录、依据展开及正常完整审核详情页。本项没有暂缓。
- 通用未来报告的前端跨报告账号统计另行处理；当前 A/B/C 账号问答仍属本轮。

## 候选代码和服务

当前分支 `codex/formal-entry-20260911`，起点 `800d8b6`，整合 C、词典及启动机制后提交 `1425652`。该提交之后的修改仍在验收，最终发布需重新冻结提交。

运行目录 `/Users/ext.wanghongtao6/Documents/Codex/runtime/xhs-audit-agent`。
候选端口 3188/8188，身份 `xhs-audit-formal-candidate`。3168 仍为现有使用入口；3148 原业务服务未切换、未重启。

候选 data 目前仅完整复制3168，加 A/B/C 报告及 C 原3轮问答。尚未合入3148两套后台全部旧会话。不要宣称候选已经包含所有旧数据。

`migration/20260911-initial/` 保存3168、3148-production、3148-demo、3151-C的 SQLite 在线备份，均做 integrity_check，manifest记录来源、表数量和哈希。它是候选核对快照，原生产仍在写入，不是最终一致迁移。

## 原库持续写入

原业务目录 `/Users/ext.wanghongtao6/Documents/Codex/projects/xhs-audit-agent-demo/data`。
80帖“维汉一家亲”运行 `investigation-run:bd4378e907614ca9af12cd8f917f7469`，Job `m3-27349eca2323d6b8307a`，会话 `investigation-session:9705f740a14e4428ad28a1c431b5a28b`。
它与C219帖无重叠。原窗口继续负责其审核，本窗口不得重复执行或迁移覆盖源库。切换前补齐增量并核对关联。

交接事实文件：原项目 `artifacts/task-plans/ethnic-discussion/handoff/report-c-20260911-112539/LIVE-AUDIT-DATA-OWNERSHIP.md`。

## 已验证

- 前端类型检查及生产构建通过。
- 43项历史问答、词典、单帖相关测试通过。
- 创建与 worker 测试41项及23个子测试通过，含迁移执行hold不被重复领取。
- 历史报告相关8项测试通过，含全部记录数量、完整快照评论、报告内容不变、无效引用拒绝。
- A/B/C 全部分析记录接口分别201/304/219；C另5帖未完成及1条缺译文。
- C点击查看研判依据打开正常TaskOutputDetailPage，报告版本与帖子引用限定读取，实际样例来源 Job `m3-206117dc6e4a9202cd10`、结果843，6条评论证据、578条保存评论。
- 流水线复用 EvidenceRelayPipeline 的已完成节点；原新增“展开依据”抽屉入口已按用户要求移除。

## 尚待完成

- 3148旧会话与源数据关联迁入；两份已生成会话来自3148-demo，失败/中断/201帖/80帖来自原生产，不能只迁其中一个库。
- 全部文件/媒体路径永久化、增量同步、旧执行队列隔离（包括JobStore启动恢复及删除归档结果逻辑）、服务重启及备份恢复。
- 新单帖真实端到端、词典多轮、A/B/C及新报告连续问答回归。
- C账号问答范围已能承认ABC授权，但原“桔子G”证据仍返回空稳定引用，正在定位桥接异常，不得声称跨报告查询验收通过。
- C历史5个未完成帖目前只显示未完成，逐帖原因尚需从真实日志接入。
- 正常详情页读取的是报告保存的完整审核结果；历史账号“关联”写操作未接入，已防止误写到同号的其他实时结果。
- 候选验收完毕后提交、固定发布代码目录、系统托管并切换唯一入口。当前尚未执行这些最终步骤。
