# 审核规则、黑话库真实编辑与 K2 补入 · 2026-09-11

入口：<http://127.0.0.1:3198/investigation?view=audit-rules>。本次在 `codex/resource-library-crud-20260911` 分支接通两个现有页面，保留原布局，增加删除确认、保存状态和错误反馈。

## 操作与存储

- 审核规则列表读取后台资源；新建、分类、规则内容、风险等级、适用阶段、豁免与启停状态，在“保存发布”时一起写入并发布。支持评论审核阶段。停用豁免不会进入实际审核提示。
- 黑话库读取后台主词、变体、标签与启停状态；名称、说明和词条编辑通过“保存配置”落库。保留原词条的标识、平台、匹配类型、风险级别及备注，标签不接受主词变体。
- 审核规则页增加整套规则删除；黑话库卡片增加删除按钮。确认后资源从当前库和新任务可选项中移除，刷新、服务重启后不恢复。已有报告使用的不可变历史版本继续保留。删除会话及其报告的范围另见 `SESSION-DELETION-20260911.md`。
- 被审核方案引用的资源暂不允许删除，界面返回引用名称；需先解除引用。保存和删除都检查读取时的版本，避免旧页面覆盖新修改；失败保留编辑内容并显示原因。重复保存以操作标识复用成功结果。

接口：`GET /api/resource-library/{ruleset|lexicon}` 及单项详情；`PUT /api/resource-library/{kind}/{id}` 接收完整 `content`、`expected_version` 和 `operation_id`，新建版本为 0；`DELETE` 接收 `expected_version`。规则发布、词库内容与保存收据在同一个资源数据库事务中完成。词库说明由兼容性迁移增加，旧编辑接口更新时保留说明。

## K2 来源与执行配置

3198 冻结数据库未包含原环境已发布的 K2。现已从原 M3 独立环境只导入 K2 聚合及其不可变版本，不导入其他任务数据。

- 名称：民族内容审核 K2：自豪表达与对象范围边界（试用）
- 标识：`ruleset.ethnic-content-review.k-trial.v2`
- 已发布版本：v1，共 13 条规则
- 内容指纹：`25ec65414796448e67f95f7de5ecbea75ae06ffb571e0750ce786d78e0e85269`

导入脚本 `scripts/import_formal_k2.py` 校验源码中固定 K2 包的指纹、不可变版本以及实际编译的 K2 专用配置。原版仍使用其固定提示与执行配置。固定 K/K2 模板修改通过“另存并发布”生成新标识，按通用规则编译执行；后端拒绝在固定标识下写入不同内容。重复导入相同来源不改数据，拒绝覆盖已改变或已删除的目标。

导入收据：运行目录 `receipts/k2-library-import-20260911.json`，包含实际来源和目标数据库路径。脚本支持 `--source-db`、`--target-db`、`--receipt`；执行前需完成数据库一致备份。

## 验证和恢复

- 初始相关回归：125 项及 19 个子测试、204 项均通过；新增资源编辑专项及 K2 补入后相关回归共 93 项及 19 个子测试通过（与前述回归有重叠，不累加）。覆盖所有权、版本冲突、保存重试、失败回滚、删除后不复活、历史快照保留、固定 K2 拒绝改写与复制后发布。
- TypeScript 和正式 Vite 构建通过；正式前后端及 worker 重启健康检查通过。
- 3198 浏览器用临时规则、词库验证新增、修改、保存、刷新、取消删除、确认删除；词库包含主词、变体、标签、停用项和说明，API 验证真实持久化。临时资源最终清理，接口返回 404。
- K2 指纹与来源一致；当前资源为 3 套规则、9 个词库。A/B/C 报告 API 内容指纹与本次操作前相同；5 个数据库完整性及外键检查通过。机器收据：运行目录 `receipts/resource-library-ui-20260911.json`。
- 本轮没有重新执行大批量采集或压力验收。

更新前 5 库一致备份：`/Users/ext.wanghongtao6/Documents/Codex/runtime/backups/formal-before-resource-library-crud-20260911`。其中同时保存资源列表与 A/B/C 报告接口指纹；完整恢复仍需冻结备份的配置、媒体、归档和依赖，参照 `FORMAL-BASELINE-20260911.md` 恢复到新目录和空闲端口。

冻结标签 `formal-baseline-20260911` 仍指向 `f3ea3b0`，本次增量不移动原标签。
