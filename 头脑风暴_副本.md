一、问题定义
系统持续运行，新用户/新帖子不断入库。当用户正在进行排序（如按违规次数降序）或筛选（如只显示高危）时，若新数据自动插入，会导致：

问题	       表现
视觉跳动	列表重绘，当前正在查看的内容被挤走，用户丢失浏览位置
逻辑崩塌	排序结果被持续破坏，用户无法完成稳定的分析
认知负担	用户需要反复定位之前正在看的目标，注意力被分散
核心矛盾：数据的持续性（不断有新数据进来）与用户操作的稳定性（排序/筛选时需要固定数据集）之间的冲突。

# 黑话语料库系统 - 数据冲突治理方案对比


## 方案A：简化双模式（开关切换）


### 一、方案概述

**核心思路**：将系统明确划分为两个独立状态，用户通过显式按钮切换。监控模式只能浏览，研判模式才能操作。新数据在研判模式下完全静默，不做任何提示。


### 二、两种模式定义

| 模式 | 核心特征 | 用户能做什么 | 新数据如何处理 |
|---|---|---|---|
| **监控模式** | 只读的实时流水 | 浏览新流入的数据，**不能排序、不能筛选** | 自动追加到列表底部（不跳动） |
| **研判模式** | 可交互的分析快照 | 对当前列表**自由排序、筛选、搜索** | 入库但**不进入当前列表**，用户无感知 |


### 三、模式切换交互
┌─────────────────────────────────────────────────────────────┐
│  ● 监控模式  │  [进入研判模式 →]                          │ ← 顶部状态切换
├─────────────────────────────────────────────────────────────┤
│  🔍 搜索框（研判模式下可用，监控模式置灰）                 │
│  [按违规排序▼] [按等级筛选▼]（仅研判模式可见）             │
├─────────────────────────────────────────────────────────────┤
│  🔴 用户A  违规89条                                       │
│  🟡 用户B  违规12条                                       │
│  🟢 用户C  违规2条                                        │
│  ...                                                     │
└─────────────────────────────────────────────────────────────┘

进入页面（默认监控模式）
    │
    ├── 数据自动追加到列表底部（可滚动浏览）
    │
    └── 用户点击「进入研判模式」
            │
            ▼
        当前列表冻结为快照
            │
            ├── 排序/筛选/搜索工具栏出现
            │
            └── 新数据入库但不在列表中显示（无提示）
                    │
                    └── 用户点击「退出研判模式」
                            │
                            ▼
                        回到监控模式
                            │
                            ├── 快照数据销毁
                            ├── 所有新数据出现在列表末尾
                            └── 恢复自动追加



### 四、状态管理

```javascript
// 核心状态（方案A）
const state = {
  mode: 'live',              // 'live' | 'snapshot'
  liveData: [],              // 全量实时数据
  snapshotData: [],          // 快照数据（深拷贝）
  filters: { sortBy: null, riskLevel: null, keyword: '' },
  selectedUserId: null,
  selectedPostId: null
}

关键操作：

操作	处理逻辑
新数据到达（监控模式）	liveData.push(data) → 视图追加到底部
新数据到达（研判模式）	仅 liveData.push(data)，不触发视图更新
进入研判模式	snapshotData = deepClone(liveData) → 显示快照 → 显示工具栏
退出研判模式	snapshotData = null → 隐藏工具栏 → 显示 liveData


五、技术选型（方案A）
层	技术选型	说明
前端框架	React 18+ / Vue 3+	任选熟悉框架
状态管理	Zustand (React) / Pinia (Vue)	轻量级状态管理，适合简单状态
虚拟滚动	TanStack Virtual / vue-virtual-scroller	支撑海量列表滚动
实时通信	WebSocket	推送新入库数据
UI组件库	Ant Design / Element Plus	提供按钮、筛选器等基础组件
数据克隆	structuredClone() / lodash.cloneDeep	进入研判模式时深拷贝快照

六、优劣势分析
维度	评价
✅ 逻辑清晰	两种模式职责单一，没有重叠
✅ 认知负担低	用户只需理解“浏览”和“分析”两个房间
✅ 开发量小	无需处理缓冲计数、横幅通知等复杂UI
✅ Bug空间小	状态管理简单，边界场景少
❌ 用户无法感知新数据	研判模式下新数据完全无提示，退出时可能突然跳变
❌ 操作路径长	需要先切模式，再做排序（2步）
❌ 新数据跳变风险	退出研判模式时，累积的新数据一次性出现，可能造成跳动
❌ 功能受限	监控模式下完全无法做任何筛选，用户可能感到不便

八、退出研判模式时平滑过渡的建议
为了避免退出时大量新数据突然出现造成的视觉跳变，建议采用分批追加策略：

text
退出研判模式时：
    │
    ├── 待追加数据 = liveData.slice(snapshotData.length)
    │
    ├── 每批追加 20 条，间隔 200ms
    │
    └── 直到所有新数据追加完毕



方案B：智能双模式（隐式切换 + 缓冲通知）
一、方案概述
核心思路：用户点击排序/筛选时自动进入研判模式（隐式切换），新数据在研判模式下累加计数并通过横幅提示，用户可主动刷新快照纳入新数据。

二、两种模式定义
模式	核心特征	进入/退出条件	新数据如何处理
监控模式（Live）	实时流水，可浏览	默认状态；点击排序/筛选时自动退出	追加到列表底部
研判模式（Snapshot）	可排序/筛选/搜索的快照	用户点击排序或筛选时自动进入	累加计数（+N），不入列；横幅提示
用户进入页面（默认监控模式）
    │
    ├── 新数据从底部追加（不跳动）
    │
    └── 用户点击「按违规次数排序」或「筛选高危」
            │
            ▼
        ✅ 自动进入研判模式（无需额外按钮）
            │
            ├── 当前列表冻结为快照
            ├── 排序/筛选立即作用于快照
            ├── 📢 +12 条新数据已缓冲 [点击更新] 横幅出现
            ├── 后续新数据持续累加计数（+12 → +23）
            ├── 用户可切换排序字段 → 基于当前快照重新排序
            ├── 用户可修改筛选条件 → 基于当前快照重新筛选
            │
            └── 用户点击「更新列表」
                    │
                    ▼
                快照刷新（旧快照 + 缓冲池）
                    │
                    ├── 重新排序
                    ├── 计数归零
                    ├── 继续冻结（保持研判模式）
                    │
                    └── 用户点击「清除筛选」
                            │
                            ▼
                        回到监控模式（快照销毁）

// 核心状态（方案B）
const state = {
  mode: 'live',              // 'live' | 'snapshot'
  liveData: [],              // 全量实时数据
  snapshotData: [],          // 快照数据（深拷贝）
  bufferCount: 0,            // 研判模式下缓冲的新数据计数
  filters: { sortBy: null, riskLevel: null, keyword: '' },
  selectedUserId: null,
  selectedPostId: null
}

五、视觉反馈设计
元素	监控模式	研判模式
工具栏	仅搜索框可见（全局搜索）	搜索框 + 排序下拉 + 筛选下拉全部可见
模式指示器	● 实时监控（绿色圆点）	⏸ 研判模式（橙色圆点）
缓冲计数横幅	不显示	固定顶部：📢 +N 条新数据已缓冲 [点击更新]
列表底部提示	N 条新内容已加载（呼吸灯）	不显示

六、边界场景处理
场景	处理方式
研判模式下，新数据持续涌入	bufferCount++，横幅数字实时更新（+12 → +23），列表不变
缓冲计数超过 999	显示 +999，不再累加
研判模式下，用户切换排序	基于当前 snapshotData（不含缓冲池）重新排序
研判模式下，用户修改筛选	基于当前 snapshotData（不含缓冲池）重新筛选
用户点击「更新列表」	snapshotData = liveData → 重新排序 → bufferCount = 0
用户点击「清除筛选」	退出研判模式 → 回到监控模式 → bufferCount = 0
用户切换用户（第一栏点击不同用户）	退出研判模式 → 回到监控模式 → 重置所有筛选状态
用户关闭页面后重开	默认进入监控模式，无缓冲数据

七、技术选型（方案B）
层	技术选型	说明
前端框架	React 18+ / Vue 3+	任选熟悉框架
状态管理	Zustand (React) / Pinia (Vue)	支持复杂状态管理，需处理快照/缓冲逻辑
虚拟滚动	TanStack Virtual / vue-virtual-scroller	支撑海量列表滚动
实时通信	WebSocket	推送新入库数据，需配合模式判断逻辑
UI组件库	Ant Design / Element Plus	提供按钮、筛选器、横幅等组件
数据克隆	structuredClone() / lodash.cloneDeep	进入研判模式时深拷贝快照


八、优劣势分析
维度	评价
✅ 操作流畅	点击排序即进入研判模式，少点一次按钮
✅ 信息感知强	用户随时知道有多少新数据等待处理
✅ 控制权在用户	是否纳入新数据由用户点击「更新列表」决定
✅ 交互精致	达到 Kibana/Grafana 级别的专业体验
✅ 未来扩展性好	缓冲池机制可直接扩展为历史版本对比、快照保存等功能
❌ 隐式切换可能造成困惑	用户可能没意识到“点排序就进入了新模式”
❌ 开发量较大	约 5 人日，是方案A的 1.7 倍
❌ 状态管理复杂	需要同时维护 liveData / snapshotData / bufferCount 三个核心状态
❌ 边界场景多	需要处理切换用户、清除筛选等场景下的状态重置


我会建议不要直接选 A 或 B，而是做一个 B 的降复杂度版本：“默认实时监控 + 用户一旦开始研判就冻结审核列表 + 顶部提示有新结果可更新”。
当前项目不是 React/Vue，也没有 WebSocket；前端是原生 JS 轮询 /api/jobs/{job_id}，每 2.5 秒把 job.items 覆盖到 currentItems 并重渲染列表：[frontend/app.js (line 496)](/Users/ext.wanghongtao6/Documents/Codex/projects/xhs-audit-agent-demo/frontend/app.js:496)、[frontend/app.js (line 519)](/Users/ext.wanghongtao6/Documents/Codex/projects/xhs-audit-agent-demo/frontend/app.js:519)。而且用户列表天然按拒绝数、复核数、证据数排序：[frontend/app.js (line 630)](/Users/ext.wanghongtao6/Documents/Codex/projects/xhs-audit-agent-demo/frontend/app.js:630)。所以即使没有显式排序按钮，新分析结果进来时也会重排，用户正在看的账号会被挤走。
我的推荐方案
保留任务状态、日志、入库/待分析统计的实时刷新；只冻结“审核研判”三栏里的用户列表、帖子列表、详情视图。
具体行为：
默认是“实时模式”
新完成的审核结果进入列表。
用户列表可以自然更新。
适合盯任务进展。

当用户发生研判动作时自动进入“研判快照”
例如点击某个用户、点击帖子、打开详情、切换到用户画像、使用搜索/筛选/排序。
此时冻结当前可见的 viewItems。
后续轮询仍继续更新 liveItems、任务状态和日志，但不改动三栏列表。

研判模式下显示轻量提示
顶部或用户列表上方显示：有 12 条新审核结果，更新列表
点“更新列表”时：viewItems = liveItems，保持当前排序/筛选条件重新计算。
点“回到实时”时：退出快照，列表重新跟随 live。

不建议“完全无提示”
方案 A 的“研判模式下用户无感知”会让审核员不知道后台是否还在产出结果。
在内容审核场景里，这种不确定感会比一个小提示更打断人。

也不建议一上来做完整方案 B
当前项目还没有复杂筛选器、虚拟列表、状态管理库。
直接引入 Zustand/Pinia、WebSocket、缓冲池会过度工程化。
用原生 JS 做 liveItems/viewItems/bufferCount/mode 就够了。

我会把它叫方案 C：轻量研判快照
核心状态可以是：
const auditView = {
  mode: "live",        // live | snapshot
  liveItems: [],
  viewItems: [],
  bufferedCount: 0,
  selectedItemKey: "",
  selectedUserKey: "",
  filters: {}
};
轮询时：
auditView.liveItems = job.items || [];

if (auditView.mode === "live") {
  auditView.viewItems = auditView.liveItems;
  renderList();
} else {
  auditView.bufferedCount = Math.max(
    0,
    auditView.liveItems.length - auditView.viewItems.length
  );
  renderSnapshotBanner();
}
还有一个我认为必须顺手改的点：当前选中帖子用的是数组下标 selectedItemIndex，这在实时追加和重排时很脆弱：[frontend/app.js (line 696)](/Users/ext.wanghongtao6/Documents/Codex/projects/xhs-audit-agent-demo/frontend/app.js:696)。最好改成稳定 ID，比如 note_id || url || local_path。否则即使做了快照，后续“更新列表”也容易选中错位。
结论
我不推荐纯 A，也不推荐完整 B。最适合这个 demo 的是 B 的交互思想 + A 的实现克制：
监控数据继续实时，研判视图一旦被用户触碰就冻结；新结果只做计数提示，由用户决定什么时候纳入。

这样能解决视觉跳动和认知负担，又不会把当前原生 JS 项目推向一套重型前端架构。





那我们需要把每次分析好的数据结果落库吗？



要。我的判断是：每条分析完成后都应该落一条独立结果记录，但不一定把所有细节都拆成很多表。
当前项目其实已经“部分落库/落盘”了：
jobs.items 会把结果列表整体写进 SQLite，但这是一个不断覆盖的大 JSON 列。
每条结果会写到 outputs/{job_id}/results/*.result.json。
contents 表里有 analyze_status 和 result_path，但它更像任务索引，不是完整审核结果库。
我建议下一步做一个轻量的 audit_results 表，作为审核页真正的数据源：
audit_results
- id
- job_id
- content_key / note_id
- author_key
- title
- url
- decision
- risk_level
- categories_json
- evidence_count
- result_json        # 完整模型输出
- result_path        # 原始 JSON 文件路径，可选
- model_name
- prompt_version
- analyzed_at
- created_seq        # 自增序号，用于前端增量拉取
这样前端的实时/快照问题会自然很多：
实时模式：轮询 created_seq > lastSeq 的新结果。
研判模式：冻结某个查询结果集，不受新结果影响。
点击“更新列表”：重新按当前筛选/排序查询最新结果。
排序/筛选可以逐步挪到后端，避免前端每次拿全量 job.items。
我不建议只依赖现在的 jobs.items 大 JSON。它适合 demo 快速展示，但后面数据一多，会有几个问题：每次更新都覆盖整包、无法高效分页/排序/筛选、崩溃恢复不够细、也不好做审核追溯。
所以结论是：每次分析完成就落库，文件保留完整原始结果，数据库保留完整 JSON + 常用索引字段。 这套最稳，也不会一开始过度设计。