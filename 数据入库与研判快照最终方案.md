# 数据入库与研判快照最终方案

## 1. 核心结论

最优方案不是单独选择“监控模式/研判模式”的前端交互，而是先把后端数据分层理顺：

1. `contents` 继续作为“内容索引与分析队列”。
2. 新增 `audit_results` 作为“已完成审核结果库”。
3. 前端审核页不再直接依赖 `jobs.items` 作为长期数据源，而是从 `audit_results` 查询结果。
4. 前端采用“实时监控 + 研判快照”的轻量双视图：
   - 实时模式：持续拉取新增审核结果。
   - 研判模式：冻结当前列表，新结果只显示计数提示，由用户主动更新。

一句话概括：

> `contents` 管“有什么内容要分析”，`audit_results` 管“分析出了什么结论”，前端用 `audit_results` 做稳定可查询的审核工作台。

## 2. 现在系统实际状态

当前链路大致是：

```text
MediaCrawler 抓取内容
        ↓
形成 crawler output / batch json
        ↓
BatchWriter 生成 batches/*.json
        ↓
IngestionStore.ingest_batch()
        ↓
contents / content_matches / ingest_batches 入 SQLite
        ↓
大模型逐条分析
        ↓
result.json 落盘
        ↓
jobs.items 更新为前端展示缓存
```

现在已经入 SQLite 的内容：

| 表 | 当前职责 |
|---|---|
| `jobs` | 任务参数、任务状态、控制状态、`items` 展示缓存 |
| `job_logs` | 任务日志 |
| `ingest_batches` | batch 文件处理状态 |
| `contents` | 内容索引、去重、分析状态、原始小 JSON 路径、结果文件路径 |
| `content_matches` | 内容与任务/关键词/分类的命中关系 |

现在还没有一张正式的“审核结果表”。完整审核结果目前主要在：

1. `outputs/{job_id}/results/{note_id}.result.json`
2. `jobs.items` 这个 JSON 字段

这对 demo 可用，但不适合作为长期工作台数据源。

## 3. 当前 `jobs.items` 的定位

`jobs.items` 不是原始帖子队列，也不是待分析队列。

它是：

```text
当前任务已经完成的大模型审核结果列表缓存
```

前端现在用它渲染：

1. 用户列表
2. 帖子列表
3. 详情页
4. 用户画像

它的问题是：

1. 每完成一条结果，都要覆盖更新整个 JSON 数组。
2. 前端每 2.5 秒轮询时会取回整包数据。
3. 不适合分页、排序、筛选、快照。
4. 不适合跨任务查询、历史分析、结果复用。
5. 作为展示缓存可以保留，但不应该继续扩展为主数据源。

## 4. 推荐目标架构

目标数据链路：

```text
MediaCrawler batch json
        ↓
raw_items/*.json
        ↓
contents 入库
  - 去重
  - 排队
  - 状态追踪
  - 原始数据定位
        ↓
大模型分析
        ↓
result.json 落盘
        ↓
audit_results 入库
  - 结构化结论
  - 完整结果 JSON
  - 查询游标
  - 前端列表数据源
```

最终职责划分：

| 层 | 职责 |
|---|---|
| `raw_items/*.json` | 原始帖子与评论快照，便于重放和排查 |
| `contents` | 内容身份、去重、分析状态、原始文件位置 |
| `audit_results` | 大模型审核结论、证据摘要、完整结果 JSON |
| `result.json` | 每条结果的完整落盘备份 |
| `jobs.items` | 兼容旧前端的临时展示缓存，后续可弱化 |

## 5. 新增 `audit_results` 表

建议新增表：

```sql
CREATE TABLE IF NOT EXISTS audit_results (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    job_id TEXT NOT NULL,
    content_id INTEGER,
    platform TEXT NOT NULL,
    content_key TEXT NOT NULL,
    note_id TEXT,
    url TEXT,
    title TEXT,
    author_key TEXT,
    author_json TEXT,
    decision TEXT,
    risk_level TEXT,
    categories_json TEXT,
    summary TEXT,
    evidence_count INTEGER NOT NULL DEFAULT 0,
    risk_image_count INTEGER NOT NULL DEFAULT 0,
    risk_frame_count INTEGER NOT NULL DEFAULT 0,
    comment_count INTEGER NOT NULL DEFAULT 0,
    result_json TEXT NOT NULL,
    result_path TEXT,
    model_text TEXT,
    model_vl TEXT,
    prompt_version TEXT,
    analyzed_at TEXT NOT NULL,
    created_at TEXT NOT NULL,
    updated_at TEXT NOT NULL,
    UNIQUE(job_id, content_key)
);

CREATE INDEX IF NOT EXISTS idx_audit_results_job_seq
ON audit_results(job_id, id);

CREATE INDEX IF NOT EXISTS idx_audit_results_job_decision
ON audit_results(job_id, decision, risk_level);

CREATE INDEX IF NOT EXISTS idx_audit_results_author
ON audit_results(job_id, author_key);
```

字段说明：

| 字段 | 用途 |
|---|---|
| `id` | 自增游标，前端增量拉取用 |
| `job_id` | 所属任务 |
| `content_id` | 关联 `contents.id` |
| `content_key` | 平台内唯一内容标识，去重用 |
| `author_key` | 用户聚合用 |
| `decision` | `pass/review/reject` |
| `risk_level` | `low/medium/high/unknown` |
| `categories_json` | 风险分类 |
| `evidence_count` | 列表排序和统计用 |
| `result_json` | 完整审核结果，方便详情页直接读取 |
| `result_path` | 结果文件路径 |
| `id` + `job_id` | 前端“新结果 +N 条”的游标依据 |

## 6. 分析完成时的写入策略

每条内容分析完成后，写入顺序建议为：

```text
1. contents.analyze_status = analyzing
2. 调用大模型生成 result
3. result.json 落盘
4. audit_results upsert
5. contents.analyze_status = completed
6. contents.result_path = result_path
7. 可选：jobs.items 继续更新，兼容旧前端
```

如果第 2 步或第 3 步失败：

```text
contents.analyze_status = failed
不写 audit_results
记录 job_logs
```

如果第 4 步写库失败：

```text
不要标记 completed
应该标记 failed 或 result_persist_failed
避免“状态显示完成但结果查不到”
```

## 7. 前端实时/快照方案

前端维护两份结果集合：

```js
const auditView = {
  mode: "live",        // live | snapshot
  liveItems: [],       // 从 audit_results 拉到的最新结果
  viewItems: [],       // 当前三栏正在展示的结果
  lastResultId: 0,     // 已拉取的最大 audit_results.id
  bufferedCount: 0,    // 研判模式下新增但未纳入视图的结果数
  selectedUserKey: "",
  selectedResultId: "",
  bloggerKeyword: "",
  userSort: "default", // default | risk
  itemSort: "default"  // default | risk
};
```

实时模式：

```text
每 2.5 秒轮询任务状态
每 2.5 秒或更长间隔拉取 audit_results 增量
新结果进入 liveItems
viewItems 跟随 liveItems 更新
不应用搜索
不应用风险排序
保持默认实时流水
```

研判模式：

```text
用户点击用户/帖子/搜索/排序时进入 snapshot
viewItems 冻结
liveItems 继续接收新增结果
bufferedCount 增加
顶部提示：有 N 条新审核结果 [更新列表]
```

用户点击“更新列表”：

```text
viewItems = liveItems
bufferedCount = 0
保持 snapshot 模式
保持当前博主 ID 搜索
保持当前显式开启的排序状态
尽量保持 selectedResultId 不变
```

用户点击“回到实时”：

```text
mode = live
viewItems = liveItems
bloggerKeyword = ""
userSort = "default"
itemSort = "default"
bufferedCount = 0
恢复默认实时流水
```

关键原则：

```text
实时模式不搜索、不排序。
进入研判快照不自动排序。
搜索和排序都是用户显式触发的研判工具。
回到实时会清空这些工具状态。
```

## 8. 推荐 API

### 8.1 任务状态接口

保留现有：

```http
GET /api/jobs/{job_id}
```

但建议逐步减少它返回的 `items`，让它主要返回：

```json
{
  "id": "job_id",
  "status": "running",
  "crawl_status": "running",
  "analysis_status": "running",
  "task_stats": {
    "ingested_count": 8,
    "pending_analysis_count": 0,
    "completed_analysis_count": 8,
    "failed_analysis_count": 0,
    "analyzing_count": 0
  },
  "logs": []
}
```

### 8.2 审核结果列表接口

新增：

```http
GET /api/jobs/{job_id}/audit-results
```

参数：

```text
after_id=0
limit=100
decision=reject
risk_level=high
author_key=xxx
keyword=xxx
sort=risk
```

返回：

```json
{
  "items": [],
  "next_after_id": 123,
  "total": 80
}
```

### 8.3 审核结果详情接口

可以先不做，列表项里直接带 `result_json`。

如果后面数据大，再新增：

```http
GET /api/audit-results/{result_id}
```

## 9. 搜索与排序建议

短期：

```text
后端增量返回当前任务的 audit_results
前端本地完成博主 ID 搜索、博主风险排序、帖子风险排序
```

适合 demo 和小数据量。

中期：

```text
后端支持分页、搜索、排序
前端只维护当前页/当前查询快照
```

适合数据量变大后。

当前阶段只建议提供三个工具：

```text
博主 ID 搜索
博主按风险值排序
帖子按风险排序
```

博主风险值定义：

```text
博主风险值 = reject 数量 × 2 + review 数量 × 1
```

用户聚合维度使用：

```text
author_key = sec_uid || user_id || user_unique_id || nickname || "unknown"
```

帖子选中态不要再用数组下标，应该使用：

```text
selectedResultId = audit_results.id
```

## 10. 和当前统计卡片的关系

前端状态卡片仍然可以来自 `contents`：

| 前端文案 | 来源 |
|---|---|
| 已入库 | `contents` 中当前任务关联内容总数 |
| 待分析 | `contents.analyze_status IN ('queued', 'failed')` |
| 已完成分析 | `contents.analyze_status = 'completed'` |
| 失败 | `contents.analyze_status = 'failed'` |
| 处理中 | `contents.analyze_status = 'analyzing'` |
| Batch | `ingest_batches` 当前任务 batch 数 |

审核列表则改成来自 `audit_results`。

这样两类数字含义清晰：

```text
contents 统计 = 管道进度
audit_results 列表 = 审核工作台结果
```

## 11. 为什么这是比 A/B 更好的方案

方案 A 的问题：

1. 研判模式完全无提示，新数据不可感知。
2. 退出研判时数据突然进入，仍可能跳动。
3. 没有解决 `jobs.items` 大 JSON 的长期问题。

方案 B 的问题：

1. 如果只在前端做缓冲，后端数据源仍然不稳定。
2. 当前项目没有前端状态管理框架，直接做完整 B 成本偏高。
3. 没有正式结果表时，分页、筛选、快照都会绕。

最终方案的优势：

1. 后端数据分层清楚。
2. 前端快照有稳定数据源。
3. 新结果计数可以基于 `audit_results.id` 精确计算。
4. 支持后续分页、筛选、排序、用户画像、历史复查。
5. 当前 demo 可以渐进迁移，不需要一次性重构所有东西。

## 12. 分阶段实施计划

### 阶段 1：补齐结果表

目标：

```text
新增 audit_results 表
分析完成后写入 audit_results
继续保留 jobs.items
前端暂时不改或少改
```

改动点：

1. `IngestionStore` 或新增 `AuditResultStore`。
2. 新增 `upsert_result(job_id, content_key, result, result_path)`。
3. pipeline 分析完成后写 `audit_results`。
4. 保持 `jobs.items` 更新，避免破坏现有页面。

### 阶段 2：前端改读结果接口

目标：

```text
审核三栏从 /api/jobs/{job_id}/audit-results 读取
任务状态仍从 /api/jobs/{job_id} 读取
```

改动点：

1. 新增结果列表 API。
2. 前端 `currentItems` 改为 `liveItems/viewItems`。
3. 选中态从 `selectedItemIndex` 改为 `selectedResultId`。
4. 用户列表基于 `viewItems` 聚合。

### 阶段 3：加入研判快照

目标：

```text
用户操作时冻结 viewItems
新结果只显示 +N
用户主动更新列表
实时模式不应用搜索和排序
```

改动点：

1. 审核页顶部增加状态条。
2. 实现 `enterSnapshotMode()`。
3. 实现 `refreshSnapshot()`。
4. 实现 `returnToLiveMode()`。
5. `returnToLiveMode()` 清空博主搜索和排序状态。

### 阶段 4：后端分页与筛选

目标：

```text
大数据量下仍然稳定
排序/筛选由数据库完成
```

改动点：

1. API 支持 `limit/offset/after_id`。
2. 支持 `decision/risk_level/author_key/keyword/sort`。
3. 前端只维护当前查询快照。

## 13. 推荐优先级

最高优先级：

1. 新增 `audit_results` 表。
2. 分析完成后写入 `audit_results`。
3. 前端选中态改为稳定 ID，不再用数组下标。

第二优先级：

1. 审核页改用 `audit_results` 接口。
2. 实现轻量快照和新结果计数。

第三优先级：

1. 后端排序筛选分页。
2. 弱化或移除 `jobs.items`。
3. 支持跨任务用户画像和历史结果复查。

## 14. 最终形态

理想最终形态：

```text
任务管理页
  - 看采集/分析进度
  - 数据来自 jobs + contents 统计

审核研判页
  - 看已完成审核结果
  - 数据来自 audit_results
  - 支持实时/快照
  - 支持排序/筛选/搜索
  - 支持稳定用户画像

数据层
  - raw_items 保存原始证据
  - contents 管内容索引与队列
  - audit_results 管审核结论
  - result.json 保存完整结果备份
```

这套设计的关键不是“多建一张表”，而是让每一层都只做一件事：

```text
采集归采集
队列归队列
结果归结果
展示归展示
```

这样实时数据不断进来时，系统可以继续跑；审核员进入研判时，视图也可以稳住。
