# 筛选排序工作方案

## 1. 目标

当前阶段只提供最必要的筛选排序能力：

1. 提供“博主按风险值排序”功能。
2. 提供“帖子按风险排序”功能。
3. 提供“博主 ID 搜索”功能。
4. 筛选排序期间保持研判快照稳定，新结果不自动插入当前视图。

注意：

> 进入研判快照不等于自动排序。快照只负责冻结当前视图；是否排序，由用户点击排序控件决定。

## 2. 核心原则

快照和排序是两件事：

```text
研判快照 = 稳定当前列表，不被新结果打断
风险排序 = 用户主动触发的列表重排能力
```

因此：

1. 用户点击博主或帖子进入快照时，保持当前列表顺序。
2. 用户输入博主 ID 搜索时，只过滤博主，不自动切换风险排序。
3. 用户点击“博主按风险排序”时，才重排博主列表。
4. 用户点击“帖子按风险排序”时，才重排当前博主的帖子列表。
5. 点击“更新列表”后，继续沿用用户当前选择的排序状态。
6. 点击“回到实时”后，退出研判工具状态，清空搜索和排序，恢复默认实时流水。

最终边界：

```text
实时模式 = 默认流水
研判模式 = 稳定视图
搜索 = 显式研判工具
排序 = 显式研判工具
```

## 3. 风险值定义

博主风险值只看两类结果：

```text
博主风险值 = 拒绝数量 × 2 + 需人工复核数量 × 1
```

对应字段：

```text
reject 数量 = decision === "reject"
review 数量 = decision === "review"
```

也就是：

```js
userRiskScore = rejectCount * 2 + reviewCount;
```

这个风险值可以展示在博主卡片上，也可以作为排序依据。

说明：

```text
reject 是明确违规，权重更高。
review 是疑似或待定风险，作为风险信号但不等同于明确违规。
pass 不计入风险值。
```

## 4. 博主列表

### 4.1 默认顺序

默认不要强制风险排序。

默认顺序就是实时流水顺序，例如：

```text
按结果进入 viewItems 的顺序聚合
```

重点是：

> 只要用户没有点击“按风险排序”，就不要因为进入快照而自动改变博主顺序。

### 4.2 风险排序

当用户点击“博主按风险排序”后，博主列表按风险值降序排列。

排序规则：

```text
1. riskScore desc
2. reject 数量 desc
3. review 数量 desc
4. 内容总数 desc
5. 最新审核结果 id desc
```

示例：

| 博主 | 拒绝 | 复核 | 通过 | 风险值 |
|---|---:|---:|---:|---:|
| A | 3 | 2 | 10 | 8 |
| B | 1 | 5 | 2 | 7 |
| C | 0 | 0 | 8 | 0 |

点击风险排序后：

```text
A 风险值 8
B 风险值 7
C 风险值 0
```

## 5. 博主 ID 搜索

只支持一个搜索框：

```text
搜索博主 ID
```

匹配字段：

```text
author_key
author.sec_uid
author.user_id
author.user_unique_id
author.short_user_id
author.nickname
```

搜索规则：

```text
ID 类字段：精确匹配
昵称字段：包含匹配
统一不区分大小写
```

匹配方式：

| 字段 | 匹配方式 |
|---|---|
| `author_key` | 精确匹配 |
| `author.sec_uid` | 精确匹配 |
| `author.user_id` | 精确匹配 |
| `author.user_unique_id` | 精确匹配 |
| `author.short_user_id` | 精确匹配 |
| `author.nickname` | 包含匹配 |

搜索行为：

1. 输入搜索词时进入研判快照。
2. 只过滤博主列表。
3. 不自动触发风险排序。
4. 如果当前已经开启“博主风险排序”，搜索结果继续按风险排序。
5. 如果当前没有开启排序，搜索结果保持原有顺序。
6. 点击“回到实时”后清空搜索词。

## 6. 帖子列表

### 6.1 默认顺序

默认不要强制风险排序。

用户选择某个博主后，帖子列表保持该博主内容在 `viewItems` 中的原始顺序。

### 6.2 风险排序

当用户点击“帖子按风险排序”后，当前博主的帖子按风险排序。

排序规则：

```text
1. decision 风险权重 desc
2. risk_level 风险权重 desc
3. evidence_count desc
4. 最新审核结果 id desc
```

权重：

```text
decision:
reject = 3
review = 2
pass = 1
unknown = 0

risk_level:
high = 3
medium = 2
low = 1
unknown = 0
```

也就是：

```text
拒绝 > 复核 > 通过
高危 > 中危 > 低危
证据多的排前面
新的结果排前面
```

## 7. 前端状态

在现有 `auditView` 上增加三个简单字段：

```js
let auditView = {
  mode: "live",
  liveItems: [],
  viewItems: [],
  lastResultId: 0,
  bufferedCount: 0,
  bloggerKeyword: "",
  userSort: "default",  // default | risk
  itemSort: "default"   // default | risk
};
```

说明：

1. `bloggerKeyword` 控制博主 ID 搜索。
2. `userSort = "risk"` 时，博主列表才按风险值排序。
3. `itemSort = "risk"` 时，帖子列表才按风险排序。
4. 进入快照不改变 `userSort` 或 `itemSort`。

## 8. 前端计算流程

### 8.1 生成展示数据

核心流程：

```text
auditView.viewItems
  ↓
按博主聚合
  ↓
按博主 ID 搜索过滤
  ↓
如果 userSort === "risk"，按博主风险值排序
否则保持默认顺序
  ↓
展示用户列表
  ↓
选中某个博主
  ↓
如果 itemSort === "risk"，该博主帖子按风险排序
否则保持默认顺序
  ↓
展示帖子列表
```

### 8.2 博主聚合

聚合结构：

```js
{
  key: authorKey,
  author,
  items,
  reject: 0,
  review: 0,
  pass: 0,
  riskScore: 0,
  latestResultId: 0,
  firstSeenOrder: 0
}
```

计算：

```js
group.riskScore = group.reject * 2 + group.review;
```

`firstSeenOrder` 用于默认顺序。

### 8.3 博主排序函数

```js
function sortUserGroups(groups, userSort) {
  if (userSort !== "risk") {
    return [...groups].sort((a, b) => a.firstSeenOrder - b.firstSeenOrder);
  }
  return [...groups].sort((a, b) => (
    b.riskScore - a.riskScore
    || b.reject - a.reject
    || b.review - a.review
    || b.items.length - a.items.length
    || b.latestResultId - a.latestResultId
  ));
}
```

### 8.4 博主搜索函数

```js
function matchesBloggerKeyword(group, keyword) {
  const q = String(keyword || "").trim().toLowerCase();
  if (!q) return true;
  const author = group.author || {};
  const exactFields = [
    group.key,
    author.sec_uid,
    author.user_id,
    author.user_unique_id,
    author.short_user_id
  ];
  const matchedId = exactFields.some(value => String(value || "").toLowerCase() === q);
  const matchedNickname = String(author.nickname || "").toLowerCase().includes(q);
  return matchedId || matchedNickname;
}
```

### 8.5 帖子排序函数

```js
function sortItems(items, itemSort) {
  if (itemSort !== "risk") {
    return [...items];
  }

  const decisionRank = item => ({
    reject: 3,
    review: 2,
    pass: 1
  }[item.decision] || 0);

  const riskRank = item => ({
    high: 3,
    medium: 2,
    low: 1
  }[item.risk_level] || 0);

  const resultId = item => Number(item.audit_result_id || item.id || 0);

  return [...items].sort((a, b) => (
    decisionRank(b) - decisionRank(a)
    || riskRank(b) - riskRank(a)
    || evidenceCount(b) - evidenceCount(a)
    || resultId(b) - resultId(a)
  ));
}
```

## 9. UI 设计

审核页顶部可以放一个简单工具条：

```text
┌──────────────────────────────────────────────┐
│ 研判视图状态条：[更新列表] [回到实时]          │
├──────────────────────────────────────────────┤
│ 博主 ID 搜索 [____________________] [清空]   │
│ 博主风险排序 [关/开]  帖子风险排序 [关/开]     │
└──────────────────────────────────────────────┘
```

控件状态：

```text
博主风险排序
  关：保持默认顺序
  开：按博主风险值排序

帖子风险排序
  关：保持默认顺序
  开：按帖子风险排序
```

排序控件必须清楚表达当前状态，不能只是一个静态按钮文案。

博主卡片建议展示：

```text
昵称 / ID
风险值 5
3 拒绝 · 2 复核 · 10 内容
```

帖子卡片继续展示：

```text
decision · risk_level
证据 N
标题 / URL
```

## 10. 快照规则

以下操作进入研判快照：

1. 点击博主。
2. 点击帖子。
3. 输入博主 ID 搜索。
4. 清空搜索。
5. 点击“博主按风险排序”。
6. 点击“帖子按风险排序”。

但是：

```text
进入研判快照本身不改变排序状态。
```

实时模式下：

```text
新结果进入 liveItems
viewItems 跟随 liveItems
保持默认展示顺序
不应用博主搜索
不应用风险排序
```

也就是说：

> 实时模式只负责看新结果持续进入；搜索和排序都属于研判动作。

研判模式下：

```text
新结果进入 liveItems
viewItems 不变
bufferedCount 增加
不改变用户列表和帖子列表
```

点击“更新列表”：

```text
viewItems = liveItems
继续应用当前博主 ID 搜索
继续应用当前 userSort 和 itemSort
bufferedCount = 0
保持研判模式
```

点击“回到实时”：

```text
mode = live
viewItems = liveItems
bloggerKeyword = ""
userSort = "default"
itemSort = "default"
bufferedCount = 0
恢复默认实时流水
```

## 11. 后端策略

当前阶段不需要后端复杂排序。

推荐：

```text
后端继续按 after_id 增量返回 audit_results
前端负责博主聚合、博主 ID 搜索、风险排序
```

接口保持：

```http
GET /api/jobs/{job_id}/audit-results?after_id={lastResultId}&limit=1000&sort=id
```

不要为了这个阶段新增很多查询参数。

原因：

1. 博主风险值是聚合结果，前端当前已经拿到结果列表，可以直接算。
2. 当前 demo 数据量不大，前端排序足够。
3. 避免把后端查询做复杂。

## 12. 最小实现步骤

1. 在审核页状态条下增加博主 ID 搜索框和清空按钮。
2. 增加“博主按风险排序”开关按钮。
3. 增加“帖子按风险排序”开关按钮。
4. `auditView` 增加 `bloggerKeyword`、`userSort`、`itemSort`。
5. 修改 `buildUserGroups()`：
   - 增加 `riskScore`
   - 增加 `latestResultId`
   - 增加 `firstSeenOrder`
   - 根据 `bloggerKeyword` 过滤
   - 只有 `userSort === "risk"` 时才按风险排序
   - `riskScore = reject * 2 + review`
6. 修改 `renderUserPool()`：
   - 展示风险值
   - 展示拒绝、复核、内容数
7. 修改 `renderContentList()`：
   - 只有 `itemSort === "risk"` 时才按风险排序
8. 搜索、清空、排序按钮点击时调用 `enterSnapshotMode()`。
9. `refreshSnapshot()` 保留当前搜索词和排序状态。
10. `returnToLiveMode()` 清空搜索词和排序状态，恢复实时流水。

## 13. 最终效果

审核页提供两个明确工具：

```text
哪个博主风险最高？  用户点击“博主按风险排序”
这个博主哪条帖子风险最高？  用户点击“帖子按风险排序”
```

但系统不会因为进入研判快照就擅自重排：

```text
快照负责稳定
排序由用户触发
搜索由用户触发
```

这样功能足够简单，也不会让用户因为“刚点进去列表就变了”而失去位置感。
