一、问题定义
系统持续运行，新用户/新帖子不断入库。当用户正在进行排序（如按违规次数降序）或筛选（如只显示高危）时，若新数据自动插入，会导致：

问题	       表现
视觉跳动	列表重绘，当前正在查看的内容被挤走，用户丢失浏览位置
逻辑崩塌	排序结果被持续破坏，用户无法完成稳定的分析
认知负担	用户需要反复定位之前正在看的目标，注意力被分散
核心矛盾：数据的持续性（不断有新数据进来）与用户操作的稳定性（排序/筛选时需要固定数据集）之间的冲突。

# 黑话语料库系统 - 数据冲突治理方案对比


## 方案A：简化双模式（开关切换）


### 一、方案概述

**核心思路**：将系统明确划分为两个独立状态，用户通过显式按钮切换。监控模式只能浏览，研判模式才能操作。新数据在研判模式下完全静默，不做任何提示。


### 二、两种模式定义

| 模式 | 核心特征 | 用户能做什么 | 新数据如何处理 |
|---|---|---|---|
| **监控模式** | 只读的实时流水 | 浏览新流入的数据，**不能排序、不能筛选** | 自动追加到列表底部（不跳动） |
| **研判模式** | 可交互的分析快照 | 对当前列表**自由排序、筛选、搜索** | 入库但**不进入当前列表**，用户无感知 |


### 三、模式切换交互
┌─────────────────────────────────────────────────────────────┐
│  ● 监控模式  │  [进入研判模式 →]                          │ ← 顶部状态切换
├─────────────────────────────────────────────────────────────┤
│  🔍 搜索框（研判模式下可用，监控模式置灰）                 │
│  [按违规排序▼] [按等级筛选▼]（仅研判模式可见）             │
├─────────────────────────────────────────────────────────────┤
│  🔴 用户A  违规89条                                       │
│  🟡 用户B  违规12条                                       │
│  🟢 用户C  违规2条                                        │
│  ...                                                     │
└─────────────────────────────────────────────────────────────┘

进入页面（默认监控模式）
    │
    ├── 数据自动追加到列表底部（可滚动浏览）
    │
    └── 用户点击「进入研判模式」
            │
            ▼
        当前列表冻结为快照
            │
            ├── 排序/筛选/搜索工具栏出现
            │
            └── 新数据入库但不在列表中显示（无提示）
                    │
                    └── 用户点击「退出研判模式」
                            │
                            ▼
                        回到监控模式
                            │
                            ├── 快照数据销毁
                            ├── 所有新数据出现在列表末尾
                            └── 恢复自动追加



### 四、状态管理

```javascript
// 核心状态（方案A）
const state = {
  mode: 'live',              // 'live' | 'snapshot'
  liveData: [],              // 全量实时数据
  snapshotData: [],          // 快照数据（深拷贝）
  filters: { sortBy: null, riskLevel: null, keyword: '' },
  selectedUserId: null,
  selectedPostId: null
}

关键操作：

操作	处理逻辑
新数据到达（监控模式）	liveData.push(data) → 视图追加到底部
新数据到达（研判模式）	仅 liveData.push(data)，不触发视图更新
进入研判模式	snapshotData = deepClone(liveData) → 显示快照 → 显示工具栏
退出研判模式	snapshotData = null → 隐藏工具栏 → 显示 liveData


五、技术选型（方案A）
层	技术选型	说明
前端框架	React 18+ / Vue 3+	任选熟悉框架
状态管理	Zustand (React) / Pinia (Vue)	轻量级状态管理，适合简单状态
虚拟滚动	TanStack Virtual / vue-virtual-scroller	支撑海量列表滚动
实时通信	WebSocket	推送新入库数据
UI组件库	Ant Design / Element Plus	提供按钮、筛选器等基础组件
数据克隆	structuredClone() / lodash.cloneDeep	进入研判模式时深拷贝快照

六、优劣势分析
维度	评价
✅ 逻辑清晰	两种模式职责单一，没有重叠
✅ 认知负担低	用户只需理解“浏览”和“分析”两个房间
✅ 开发量小	无需处理缓冲计数、横幅通知等复杂UI
✅ Bug空间小	状态管理简单，边界场景少
❌ 用户无法感知新数据	研判模式下新数据完全无提示，退出时可能突然跳变
❌ 操作路径长	需要先切模式，再做排序（2步）
❌ 新数据跳变风险	退出研判模式时，累积的新数据一次性出现，可能造成跳动
❌ 功能受限	监控模式下完全无法做任何筛选，用户可能感到不便

八、退出研判模式时平滑过渡的建议
为了避免退出时大量新数据突然出现造成的视觉跳变，建议采用分批追加策略：

text
退出研判模式时：
    │
    ├── 待追加数据 = liveData.slice(snapshotData.length)
    │
    ├── 每批追加 20 条，间隔 200ms
    │
    └── 直到所有新数据追加完毕



方案B：智能双模式（隐式切换 + 缓冲通知）
一、方案概述
核心思路：用户点击排序/筛选时自动进入研判模式（隐式切换），新数据在研判模式下累加计数并通过横幅提示，用户可主动刷新快照纳入新数据。

二、两种模式定义
模式	核心特征	进入/退出条件	新数据如何处理
监控模式（Live）	实时流水，可浏览	默认状态；点击排序/筛选时自动退出	追加到列表底部
研判模式（Snapshot）	可排序/筛选/搜索的快照	用户点击排序或筛选时自动进入	累加计数（+N），不入列；横幅提示
用户进入页面（默认监控模式）
    │
    ├── 新数据从底部追加（不跳动）
    │
    └── 用户点击「按违规次数排序」或「筛选高危」
            │
            ▼
        ✅ 自动进入研判模式（无需额外按钮）
            │
            ├── 当前列表冻结为快照
            ├── 排序/筛选立即作用于快照
            ├── 📢 +12 条新数据已缓冲 [点击更新] 横幅出现
            ├── 后续新数据持续累加计数（+12 → +23）
            ├── 用户可切换排序字段 → 基于当前快照重新排序
            ├── 用户可修改筛选条件 → 基于当前快照重新筛选
            │
            └── 用户点击「更新列表」
                    │
                    ▼
                快照刷新（旧快照 + 缓冲池）
                    │
                    ├── 重新排序
                    ├── 计数归零
                    ├── 继续冻结（保持研判模式）
                    │
                    └── 用户点击「清除筛选」
                            │
                            ▼
                        回到监控模式（快照销毁）

// 核心状态（方案B）
const state = {
  mode: 'live',              // 'live' | 'snapshot'
  liveData: [],              // 全量实时数据
  snapshotData: [],          // 快照数据（深拷贝）
  bufferCount: 0,            // 研判模式下缓冲的新数据计数
  filters: { sortBy: null, riskLevel: null, keyword: '' },
  selectedUserId: null,
  selectedPostId: null
}

五、视觉反馈设计
元素	监控模式	研判模式
工具栏	仅搜索框可见（全局搜索）	搜索框 + 排序下拉 + 筛选下拉全部可见
模式指示器	● 实时监控（绿色圆点）	⏸ 研判模式（橙色圆点）
缓冲计数横幅	不显示	固定顶部：📢 +N 条新数据已缓冲 [点击更新]
列表底部提示	N 条新内容已加载（呼吸灯）	不显示

六、边界场景处理
场景	处理方式
研判模式下，新数据持续涌入	bufferCount++，横幅数字实时更新（+12 → +23），列表不变
缓冲计数超过 999	显示 +999，不再累加
研判模式下，用户切换排序	基于当前 snapshotData（不含缓冲池）重新排序
研判模式下，用户修改筛选	基于当前 snapshotData（不含缓冲池）重新筛选
用户点击「更新列表」	snapshotData = liveData → 重新排序 → bufferCount = 0
用户点击「清除筛选」	退出研判模式 → 回到监控模式 → bufferCount = 0
用户切换用户（第一栏点击不同用户）	退出研判模式 → 回到监控模式 → 重置所有筛选状态
用户关闭页面后重开	默认进入监控模式，无缓冲数据

七、技术选型（方案B）
层	技术选型	说明
前端框架	React 18+ / Vue 3+	任选熟悉框架
状态管理	Zustand (React) / Pinia (Vue)	支持复杂状态管理，需处理快照/缓冲逻辑
虚拟滚动	TanStack Virtual / vue-virtual-scroller	支撑海量列表滚动
实时通信	WebSocket	推送新入库数据，需配合模式判断逻辑
UI组件库	Ant Design / Element Plus	提供按钮、筛选器、横幅等组件
数据克隆	structuredClone() / lodash.cloneDeep	进入研判模式时深拷贝快照


八、优劣势分析
维度	评价
✅ 操作流畅	点击排序即进入研判模式，少点一次按钮
✅ 信息感知强	用户随时知道有多少新数据等待处理
✅ 控制权在用户	是否纳入新数据由用户点击「更新列表」决定
✅ 交互精致	达到 Kibana/Grafana 级别的专业体验
✅ 未来扩展性好	缓冲池机制可直接扩展为历史版本对比、快照保存等功能
❌ 隐式切换可能造成困惑	用户可能没意识到“点排序就进入了新模式”
❌ 开发量较大	约 5 人日，是方案A的 1.7 倍
❌ 状态管理复杂	需要同时维护 liveData / snapshotData / bufferCount 三个核心状态
❌ 边界场景多	需要处理切换用户、清除筛选等场景下的状态重置